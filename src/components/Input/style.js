import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    input:{
        borderWidth: 1,
        borderColor:'#666',
        borderRadius: 8,
        width: 300,
        height: 50,
        padding: 10,
    }
})